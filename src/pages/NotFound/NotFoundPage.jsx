export default function NotFoundPage() {
  return (
    <section className="flex justify-center w-full min-h-screen">
      <div className="flex w-full font-extrabold text-center">
        <span className="text-base text-center text-primary-950/70">404</span>
        <span>Pagina No Encontrada</span>
        <span>Lo sentimos, la pagina que desea buscar no se encuentra</span>
      </div>
      <div>
        
      </div>
    </section>
  );
}
